{
  "name": "xeno-crm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w message-simulator\" \"npm run dev -w frontend\"",
    "dev:backend": "concurrently \"npm run dev -w backend\" \"npm run dev -w message-simulator\"",
    "dev:frontend": "npm run dev -w frontend",
    "seed": "npm run seed -w backend",
    "db:setup": "npm run db:setup -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
