# ---------------------------------------------------------
# MESSAGE SIMULATOR ENVIRONMENT VARIABLES (Render)
# ---------------------------------------------------------

PORT=3002

# Note: You do NOT need to hardcode a CRM_RECEIPT_URL here!
# The backend will dynamically pass its own callback URL
# in the payload of every message it sends to this simulator.
